# Desktop E2E (Playwright)

Paranoid Encryption file upload scenario.

```bash
npm run test:e2e-desktop -- --setup "CoreParanoidEncryptionWebclientPlugin Chrome"
```

Optional: `E2E_PARANOID_PASSWORD` (defaults to `e2e-paranoid-test`).

Stand gate: Paranoid Encryption settings tab or encrypt-on-upload dialog missing.
